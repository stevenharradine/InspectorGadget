{
  "name": "inspector-gadget",
  "description": "Will verify a system is running correctly",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "express": "*",
    "cluster": "*",
    "execSync": "*"
  }
}
